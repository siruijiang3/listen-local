# Qwen3-TTS：原版性能对照与边听边生成方案

日期：2026-09-23。用户已认可 0.6B 本轮听感，认为情感表达不如 1.7B 充沛，但更自然。建议 0.6B CustomVoice 作为下一阶段主候选，保留 1.7B 作对照；这不代表量化版、浏览器版或未试听语言已通过。

后续加速研究与原版阶段诊断见 [QWEN-ACCELERATION-RESEARCH.md](QWEN-ACCELERATION-RESEARCH.md)：已找到 CUDA Graphs 的具体原生候选和 WebGPU 对应优化方向，尚未在本机复现加速引擎成绩。

## 测量口径

硬件：NVIDIA RTX 4060 Laptop，nvidia-smi 报告 8188 MiB 显存。官方 qwen-tts 0.1.1，PyTorch 2.6.0+cu124，CUDA、BF16、SDPA，6 个 CPU 线程、单请求、seed 42、原始默认采样、空风格指令。没有安装新推理引擎或改变系统电源配置。

历史记录来自两个版本各 18 条样音的 manifest；不含下载和加载。两轮分开生成、环境波动未隔离，不能用总耗时宣布模型的固有速度排名。

本次补测脚本：`scripts/benchmark-qwen-runtime.py`。两个模型在独立 Python 进程依次加载，先用相同中文句子预热，再生成一个短开头、同一中文短文、同一英文短文。每例仅测一次，不是统计性能评测。计时包含实际同步等待和解码，排除文件编码、播放和下载；非流式接口返回完整数组，不是首包延迟测量。

原始结果：`.qa/qwen-benchmark-0.6B.json`、`.qa/qwen-benchmark-1.7B.json`。每秒 nvidia-smi 采样并保存功率/时钟/整卡显存；采样峰值可能漏掉短暂尖峰。监测开销包含在两版同样的测试条件中。PyTorch allocated 是张量占用，reserved 是分配器保留池；整卡占用包含桌面和其他应用。不能相加，也不能把整卡数据直接视为模型最低配置。

## 显存与文件大小

| 指标 | 0.6B | 1.7B |
| --- | ---: | ---: |
| 已下载完整模型目录，十进制 GB | 2.50 | 4.52 |
| 历史 18 条样音 PyTorch allocated 峰值，GiB | 2.71 | 4.59 |
| 补测 allocated 峰值，GiB | 2.65 | 4.58 |
| 补测 reserved 峰值，GiB | 3.04 | 5.01 |
| 补测前整卡占用，GiB | 1.89 | 1.86 |
| 补测整卡采样峰值，GiB | 5.02 | 7.01 |
| 整卡峰值减基线，GiB，近似增量 | 3.12 | 5.15 |

0.6B 历史张量峰值较 1.7B 低约 41%。这明确支持选小模型，但不能据此推导 WebGPU/统一内存设备的具体内存需求。

## 时间

RTF = 生成耗时 / 音频时长。1 倍速持续播放至少要求长期 RTF < 1，并留出处理与调度余量。

| 历史 18 条样音 | 0.6B | 1.7B |
| --- | ---: | ---: |
| 总音频时长 | 1301.44 秒 | 1283.68 秒 |
| 总生成耗时 | 2387.74 秒 | 2143.66 秒 |
| 加权 RTF | 1.835 | 1.670 |
| 中文女声长文 RTF | 1.602 | 1.678 |
| 中文男声长文 RTF | 1.637 | 1.659 |
| 英文长文 RTF | 1.707 | 1.682 |

| 本次补测（生成耗时 / 音频时长） | 0.6B | 1.7B |
| --- | ---: | ---: |
| 中文开头：雨停的时候，天还没有完全亮。 | 14.89 / 2.72 秒 | 4.12 / 2.40 秒 |
| 中文短文 Serena | 93.85 / 22.08 秒 | 39.33 / 23.52 秒 |
| 英文短文 Aiden | 34.89 / 20.72 秒 | 35.74 / 19.84 秒 |

本次从磁盘加载模型分别耗时 6.58 / 4.54 秒，不含 Python 启动、首次下载及预热。历史首次下载分别 47.78 / 66.51 秒，只代表当时网络条件。

0.6B 中文补测较慢，与其长文历史表现差异明显，根因尚未定位。没有证据将其简单归因于功耗、模型结构或量化问题；不能承诺模型更小就更快。当前实现的持续速度不符合边听边生成要求。以 RTF 1.6 为例，10 秒音频要生成 16 秒；有限预缓冲只能延后卡顿，不能长期消除缺口。

## 实现边界与公开资料

本地安装包 `qwen_tts/inference/qwen3_tts_model.py` 的 `generate_custom_voice` 文档明确：`non_streaming_mode=False` 模拟流式文本输入，并不会开启真正的流式生成；该方法先获得所有 codec，再统一 decode 并返回 WAV 数组。不能只改一个布尔参数便宣称流式播放。

- [Qwen 官方仓库](https://github.com/QwenLM/Qwen3-TTS)：模型架构支持流式；官方 97ms 宣传值不是本机或浏览器实测。
- [0.6B CustomVoice ONNX 文件目录](https://huggingface.co/onnx-community/Qwen3-TTS-12Hz-0.6B-CustomVoice/tree/main)：已有 CPU/CUDA、不同精度的导出。不是经过本项目验证的 ONNX Runtime Web/WebGPU 即用包；目录合计大小也不是单一运行配置下载量。
- [qwen3-tts-onnxruntime](https://github.com/zhangluoyang/qwen3-tts-onnxruntime)：提供 Python/C++ 流式实现，可作移植参考，尚未在本机验收。
- [llama-web-bridge](https://github.com/leehack/llama-web-bridge)：项目报告浏览器 WebGPU/memory64 实验支持，所验证的是 1.7B Base 量化模型组合；不是本次 0.6B CustomVoice 和已认可的预设声音。不能直接替换。
- [原生 C 实现](https://github.com/gabriele-mastrapasqua/qwen3-tts)：报告优化和量化后的实时性能，作为本地运行备选资料；其速度不是本机测量，也不证明 WebGPU 可同速运行。

## 建议下一阶段：先完成可用性验证，再接整本书

继续优先纯静态、浏览器本机推理、同一 0.6B CustomVoice 模型，不默认增加模型、服务端费用或本地安装要求。

1. 做 0.6B 浏览器推理最小原型：完整检查文本 tokenizer、talker、code predictor、音频 decoder 的 WebGPU 算子与缓存支持。先建立与 BF16 原版的高精度对照，再评估 INT8/混合精度，必要时才试 INT4；不同精度必须重新试听。文件变小不保证更快。保留 Serena/Uncle_Fu/Aiden。
2. 改为真正逐块返回音频：完整自然句/段作为文本上下文，在模型生成过程中增量解码 PCM，维持必要的 decoder 状态/重叠裁剪，不独立解码造成拼接毛刺。可从 0.5–1 秒音频块试起，以实测确定。首块直接交给 AudioWorklet 队列，不等整章、整条 WAV 或整段 MP3 编完。试听脚本的整条 RMS 归一化不可阻塞首包，需改为固定增益和有界限幅并复核音质。
3. 产品管线：本地选择 EPUB/PDF → 优先解析当前章节并展示 → 模型就绪后合成当前位置 → 播放同时预生成后文。首段在自然边界保持较短，后续按句/段合并保留韵律；避免逐字切割。用固定文本范围与版本/声音/精度的键保证片段不漏不重。
4. 自适应预读：按实测生成速度和播放速度维护约 20–60 秒的目标音频储备，启动不必等储备全部填满；达到上限暂停生成。跳章取消旧队列并优先新位置。OPFS 保存音频，IndexedDB 保存书籍、范围、状态与游标；内存只保留有界播放窗口，导出排低优先级。
5. 分开显示首次模型下载、磁盘/缓存加载、首个可播放音频、持续生成速度。2.50 GB 原版首次下载不可隐藏成“几秒启动”；缓存后才测日常启动体验。量化包的实际体积按最终构件统计。

建议验收目标（尚未达到）：权重已缓存、模型已就绪时，点击到首个可听音频 P95 ≤ 3 秒；1 倍速连续 30 分钟无缓冲卡顿；持续 RTF ≤ 0.7–0.8；报告最大生成间隙与实际卡顿次数，不能仅看平均 RTF；长时间内存有界；已认可声音听感不过度退化。1.5 倍速需 RTF < 0.667 且额外余量，不能沿用 1 倍速门槛。模型冷加载另行测量。

如果浏览器原型在目标电脑上仍达不到门槛，再由用户决定是否接受网页加本地运行组件。它仍可免费、开源、无云端推理，但需要一次安装，改变原来的纯静态产品约束。公共云端推理会产生持续成本，不作为无条件免费替代。

本轮只完成性能比较、补测与方案；未宣称已交付浏览器实时生成。

# 验证记录

## 2026-09-22 普通话修订（mandarin-fix-2）

- 移除用户否决的晓北、晓妮。下面旧版运行表只证明旧链路运行过，不是这些声音的普通话合格证据。
- 本轮 24 项测试、TypeScript/Vite 构建和格式检查通过。10 条 v1.1 中文句子的音节和声调与 Misaki 0.9.4 参考一致；忽略分词分隔符，未验证韵律等价。
- 浏览器实际生成《雨后的书店》：v1.1 / 001 完成，纯生成 1.9 秒；v1.0 / 晓晓完成，2.4 秒；v1.0 / 云扬完成，2.4 秒。001 音频 16.3 秒，云扬音频 20.95 秒，媒体 readyState=4、error=null。这些仅是生成/解码结果，未填入用户听感评分。
- 更新缓存后，新候选及默认选择在页面生效；旧中文前端的记录不会出现在修订后的试听卡上。历史数据保留，重开页面可恢复新生成记录。
- 内置浏览器点击远程官方参考录音、以及随后点击本地生成的音频播放，分别发生页面崩溃，原因未确定。恢复页面后记录仍在；参考入口现为官方样音网页链接。**当前环境仅验证生成和解码，未通过实际播放兼容性验收**；需在独立 Chrome/Edge 中继续核实。没有下载/转码第三方音频来绕过浏览器限制。
- 新声音尚未通过用户普通话听感审核。详见 [普通话核实记录](MANDARIN-AUDIT.md)。

## 修订前的运行记录

2026-09-22，本机 Windows，Chromium 153；浏览器报告 32 个逻辑核、32 GB deviceMemory（估计值，不是完整硬件识别）。GPU 型号未采集。

## 自动检查

- TypeScript 与 Vite 生产构建通过。
- 12 个测试通过：原文分段完整性、Unicode 边界、样本覆盖、WAV 数据/合并/采样率、无效音频、响度上限、中文声调与轻声、所有内置短文与长文的固定词表兼容性、中英混读。
- npm 安装检查：0 个已知漏洞（固定 Vite 6.4.3、Vitest 4.1.11）；依赖审计不是安全保证。
- 源码格式检查通过。

## 浏览器实际生成

所有音频均由页面 Worker 真实生成，没有用录音占位。下表只记录运行行为，**不表示自然度、准确性或用户审核已通过**。

| 路径 | 短文 | 长文 | 运行方式 |
| --- | --- | --- | --- |
| Kokoro v1.0 / 晓北 | 完成 | 64 段，384.7 秒音频，47.4 秒生成 | WebGPU，允许 CPU 算子回退 |
| Kokoro v1.1-zh / 001 | 完成 | 64 段，229.1 秒音频，28.4 秒生成 | WebGPU，允许 CPU 算子回退 |
| Piper / Huayan medium | 完成 | 64 段完成，75.0 秒生成 | WASM，单线程 |
| Kokoro v1.0 / Heart | 完成 | 59 段，249.2 秒音频，34.3 秒生成 | WebGPU，允许 CPU 算子回退 |
| Piper / Lessac high | 完成 | 59 段，210.3 秒音频，313.8 秒生成 | WASM，单线程 |

长文中三款中文路径和 Kokoro 英文的页面 JS 堆采样峰值约 21–56 MB。该值不包含所有 Blob、Worker、WASM、显存，也没有测量完整进程峰值，不能据此宣布通过整本书内存验收。

## 功能与审核边界

- 已检查桌面页面布局、语言切换、相同文本串行生成、首段预览及完整 WAV 媒体元数据。
- WAV 下载按钮、笔记录入和本地保存已通过页面操作检查；功能自测笔记明确标识为非用户听感评价。
- 刷新后 11 条当时的历史记录恢复；英文长文自测笔记内容仍在。音频播放器 readyState=4、解码 error=null。
- 取消后立即停止 Worker 并显示已停止；随后可重新启动生成。
- 停止本地预览服务器后，刷新页面成功，并再次生成三款中文短文；三条记录均显示新下载 0.0 MB。此项验证了页面/运行库/模型缓存重用，未通过操作系统切断全部互联网，也未覆盖所有声音组合。
- 生成结果的音频长度因模型而异；没有自动语音识别逐句校对，漏读/重复/错音/停顿必须人工对照原文。
- 中文浏览器前端不是官方 Python 参考实现的逐字复刻，结果只能代表本页面的组合。
- Huayan 训练数据许可 Unknown；不得在最终选型里当作已清理许可的声音。
- 多语言后续样音、PDF/EPUB、整本书断点、OPFS、MP3/ZIP、公网部署及完整断网/跨浏览器兼容矩阵仍属于后续工作，不在当前阶段声称验收完成。

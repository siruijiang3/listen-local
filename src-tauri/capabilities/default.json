{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Local desktop window and explicit file selection only",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
